## Meta Generator Modules

**Status:** ACTIVE MODULE

`modules` erzeugt wiederholbare Modul-Scaffolds fuer interne Generator-Workflows.

Aktuelle reale Nutzung:

- wird vom internen Workflow `scripts/devlab/preview_meta_generator_seed_bundle.mjs` konsumiert
- wird vom internen Workflow `scripts/devlab/preview_app_scaffold_bundle.mjs` konsumiert

Die Zone bleibt bewusst strukturiert: wiederholbare Scaffolds, keine allgemeine Codegen-Maschine.
