## Meta Generator Assets

**Status:** ACTIVE SEED MODULE

`assets` erzeugt strukturierte Asset-Spezifikationen fuer interne Vorschau- und Generator-Workflows.

Aktuelle reale Nutzung:

- wird vom internen Workflow `scripts/devlab/preview_meta_generator_seed_bundle.mjs` konsumiert

Die Zone ist aktiv, aber bewusst auf reproduzierbare Spezifikationen begrenzt.
