## Meta Generator Assets

**Status:** ACTIVE MODULE

`assets` erzeugt strukturierte Asset-Presets fuer Export- und Generator-Workflows.

Aktuelle reale Nutzung:

- wird vom internen Workflow `scripts/devlab/preview_meta_generator_seed_bundle.mjs` konsumiert
- liefert Export-Presets an `shared/application/frontend_os/export_runtime.js`

Die Zone bleibt bewusst auf reproduzierbare Spezifikationen begrenzt.
