## Meta Generator Agent Adapters

**Status:** ACTIVE SEED MODULE

`agent_adapters` baut standardisierte Adapter-Requests fuer interne Generator-Workflows.

Aktuelle reale Nutzung:

- wird vom internen Workflow `scripts/devlab/preview_meta_generator_seed_bundle.mjs` konsumiert

Die Zone ist aktiv, aber noch kein breiter produktiver Agenten-Orchestrator.
