## Meta Generator Content

**Status:** ACTIVE MODULE

`content` erzeugt strukturierte Content-Bundles und Markdown-Artefakte auf Basis der freigegebenen Generator-Blueprints.

Aktuelle reale Nutzung:

- erzeugt die Post-v3-Roadmap
- wird vom internen Workflow `scripts/devlab/generate_post_v3_roadmap.mjs` konsumiert
- liefert Surface- und Dimension-Copy an `shared/application/frontend_os/discoverability_runtime.js`
- liefert Export-Copy an `shared/application/frontend_os/export_runtime.js`

Die Zone ist aktiv und produktiv konsumiert, bleibt aber strikt strukturiert statt frei textmagisch.
