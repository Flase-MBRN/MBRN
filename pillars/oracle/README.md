# /pillars/oracle/

**Status:** PARTIALLY ACTIVE

Dieses Pillar ist bewusst nur teilweise aktiv.

Aktiv heute:

- `browser_read/` für Oracle-Snapshots in der Browser-Runtime

Bewusst noch nicht in dieses Pillar umgehängt:

- Heavy-Processing
- Fusion
- Signals
- Snapshots-Persistenz
- Backtesting-Logik

Die operative Rechen- und Pipeline-Substanz lebt aktuell weiter unter:

- `scripts/oracle/`
- `scripts/pipelines/`
