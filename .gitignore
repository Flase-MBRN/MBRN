archive/
.gemini/
*.log
.DS_Store
node_modules/
tmp/
*.pdf
MBRN_DEEP_DECODE_*.pdf
.obsidian/

# Security: Environment secrets - NEVER COMMIT REAL KEYS
# Core environment files
.env
.env.*
*.local
config.local.js

# Backend/Script Environment Secrets (NEVER commit these)
# Only backend .env files with real secrets are ignored
scripts/**/.env
scripts/**/.env.*
**/*.local
config.local.js

# NOTE: Frontend env.js is TRACKED and contains PUBLIC Supabase Anon-Key
# This is required for static GitHub Pages hosting - NOT a secret!

# Data Cache & Temp Files (SECURITY: Prevent data leaks)
AI/models/data/*.json
*.cache.json
.temp/
.supabase/
coverage/

# Backup files (SECURITY: Prevent code/secret leaks via backups)
**/*.bak