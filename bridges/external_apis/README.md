# /bridges/external_apis/

**Status:** RESERVED / NOT IMPLEMENTED

Dieser Ordner ist der reservierte Sammelpunkt für externe HTTP- und Provider-Integrationen außerhalb von Commerce.

Aktuell gibt es hier bewusst keine aktive Runtime-Implementierung.

Regel:

- echte externe IO gehört später hierher
- bis dahin bleibt der Ordner markiert und leer
- keine Test- oder Fake-Businesslogik nur zur optischen Befüllung
